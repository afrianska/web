---
import ContactLayout from "../layouts/ContactLayout.astro";
import HeaderContact from "../components/HeaderContact.astro";
---

<ContactLayout pageTitle='Book A Call Afrian'>
  <main id='home' class='w-full h-full mx-auto'>
    <HeaderContact />
    <section class='flex flex-col justify-center h-fit w-full py-16'>
      <div
        class='w-screen h-auto mx-auto'
        style='width:100%;height:100%;overflow:scroll'
        id='my-cal-inline'
      >
      </div>
    </section>
  </main>
  <section class='w-full'></section>
</ContactLayout>

<!-- Cal inline embed code begins -->

<script type='text/javascript'>
  (function (C, A, L) {
    let p = function (a, ar) {
      a.q.push(ar);
    };
    let d = C.document;
    C.Cal =
      C.Cal ||
      function () {
        let cal = C.Cal;
        let ar = arguments;
        if (!cal.loaded) {
          cal.ns = {};
          cal.q = cal.q || [];
          d.head.appendChild(d.createElement("script")).src = A;
          cal.loaded = true;
        }
        if (ar[0] === L) {
          const api = function () {
            p(api, arguments);
          };
          const namespace = ar[1];
          api.q = api.q || [];
          if (typeof namespace === "string") {
            cal.ns[namespace] = cal.ns[namespace] || api;
            p(cal.ns[namespace], ar);
            p(cal, ["initNamespace", namespace]);
          } else p(cal, ar);
          return;
        }
        p(cal, ar);
      };
  })(window, "https://app.cal.com/embed/embed.js", "init");
  Cal("init", "15min", { origin: "https://cal.com" });

  Cal.ns["15min"]("inline", {
    elementOrSelector: "#my-cal-inline",
    config: { layout: "month_view" },
    calLink: "afrian/15min",
  });

  Cal.ns["15min"]("ui", {
    styles: { branding: { brandColor: "#000000" } },
    hideEventTypeDetails: false,
    layout: "month_view",
  });
</script>
<!-- Cal inline embed code ends -->
